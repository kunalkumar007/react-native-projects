import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function Card(props: { icon: string; title: string; bg: string; number: string }) {
	return (
		<View>
			<Text>Card</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
